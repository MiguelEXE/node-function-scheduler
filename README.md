# node-function-scheduler
A task scheduler for generator functions

## Building:
For npm:

`npm run build`

For yarn:

`yarn build`

## Running:
Running without passing any files will open a modified version of a REPL session.

Otherwise will run the scheduler.